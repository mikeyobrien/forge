# Environment configuration for MCP Server

# Required: Base directory for all document storage
CONTEXT_ROOT=/path/to/your/documents

# Optional: Logging level (debug, info, warn, error)
# Default: info
LOG_LEVEL=info

# Optional: Server port for HTTP transport
# Default: 3000
PORT=3000

# Optional: Node environment (development, production, test)
# Default: production
NODE_ENV=development